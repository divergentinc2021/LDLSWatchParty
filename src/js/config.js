// ============================================
// Configuration
// ============================================

// App URL (auto-detected)
export const APP_URL = window.location.origin;

// Room settings
export const ROOM_SETTINGS = {
  codeLength: 5,
  maxParticipants: 8
};
