// ============================================
// Configuration
// ============================================

// Apps Script API URL - Replace with your deployment URL
export const API_URL = 'https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec';

// App URL (auto-detected)
export const APP_URL = window.location.origin;

// Room settings
export const ROOM_SETTINGS = {
  codeLength: 5,
  maxParticipants: 8,
  tokenLength: 10
};
